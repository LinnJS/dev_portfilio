<%= f_with(model: project_item, local: true) do |f| %>
  <% if project_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_item.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% project_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :subtitle %>
    <%= f.text_area :subtitle %>
  </div>

    <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <ul>
    <%= f.fields_for :technologies do |technology_f| %>
    <li>
      <%= technology_f.label :name %>
      <%= technology_f.text_field :name %>
    </li>

    <% end %>
  </ul>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
